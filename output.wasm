(module
  ;; Imports
  (import "env" "memory" (memory 1))
  
  ;; Generated members
;; class Test
(type $Test (struct
(func $NoParams
(block
(return)
  ;; Deallocation instructions inserted here based on AutomaticModel analysis
)
  (result i32)

)
(func $WithParams
(param $ ), 
  (result i32)
(block
(return)
  ;; Deallocation instructions inserted here based on AutomaticModel analysis
)
)
))
)