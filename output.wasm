(module
  ;; Imports
  (import "env" "memory" (memory 1))
  
  ;; Generated members
;; class Test
(type $Test (struct
(func $Add
  ;; Parameters and body
  (result i32)
(param $(block
(return)
  ;; Deallocation instructions inserted here based on AutomaticModel analysis
) {type}), (block
(return)
  ;; Deallocation instructions inserted here based on AutomaticModel analysis
)
)
))
)